# Replace <REGISTRY> with your image registry, e.g. us.icr.io/<namespace>/reviews-api:latest
apiVersion: apps/v1
kind: Deployment
metadata:
  name: reviews-api
  namespace: cardealer
spec:
  replicas: 2
  selector:
    matchLabels:
      app: reviews-api
  template:
    metadata:
      labels:
        app: reviews-api
    spec:
      containers:
        - name: reviews-api
          image: <REGISTRY>/reviews-api:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3000
              name: http
          env:
            - name: PORT
              value: "3000"
            - name: MONGO_URL
              value: mongodb://mongo:27017/reviews
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: jwt-secret
                  key: JWT_SECRET
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 3
            periodSeconds: 5
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 256Mi
---
apiVersion: v1
kind: Service
metadata:
  name: reviews-api
  namespace: cardealer
spec:
  selector:
    app: reviews-api
  ports:
    - port: 3000
      targetPort: 3000
      name: http
  type: ClusterIP
