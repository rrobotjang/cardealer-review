# Shared JWT secret used by reviews-api and backend.
# DEV ONLY: replace with a real secret in production (kubectl create secret ... / oc create secret ...)
apiVersion: v1
kind: Secret
metadata:
  name: jwt-secret
  namespace: cardealer
type: Opaque
data:
  JWT_SECRET: c3VwZXItc2VjcmV0LWtleS1mb3ItZGV2  # base64("super-secret-key-for-dev")
